services:
  ffmpeg-builder:
    build:
      context: .
      args:
        - TARGET_OS=linux
        - TARGET_ARCH=x86_64
    image: javiermk/vidra-ffmpeg:latest
    container_name: vidra-ffmpeg-builder
    volumes:
      - ./dist:/dist
      - ./config.sh:/config.sh:ro
      - ./compile.sh:/compile.sh:ro
    entrypoint: ["/compile.sh", "linux", "x86_64"]